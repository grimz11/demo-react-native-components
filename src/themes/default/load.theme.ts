import { space } from './space.theme';
import { size } from './size.theme';
import { font, fontSize, fontWeight } from './font.theme';
import { palette } from './palette.theme';

export const DefaultTheme = {
  palette,
  font,
  fontWeight,
  fontSize,
  size,
  space,
};
